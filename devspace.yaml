name: valid8or-plugin-aws
version: v2beta1

vars:
  IMAGE: quay.io/spectrocloud-labs/valid8or-plugin-aws

images:
  valid8or-plugin-aws:
    image: ${IMAGE}
    tags:
    - dev
    createPullSecret: false
    dockerfile: ./Dockerfile.devspace
    rebuildStrategy: ignoreContextChanges
    docker: {}

deployments:
  valid8or-plugin-aws:
    namespace: valid8or-plugin-aws-system
    kubectl:
      kustomize: true
      manifests:
      - config/devspace

dev:
  valid8or-plugin-aws:
    imageSelector: ${IMAGE}
    terminal:
      enabled: true
      disableReplace: true
      workDir: /workspace
      command: ./devspace-start.sh
    ports:
    - port: "2343"
    sync:
    - path: .:/workspace
      excludePaths:
        - '**'
        - '!/pkg'
        - '!/api'
        - '!/cmd'
        - '!/config'
        - '!/internal'
        - '!/devspace-start.sh'
        - '!/hack'
        - '!/go.mod'
        - '!/go.sum'
        - '!/main.go'
        - 'Makefile'
